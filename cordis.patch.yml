# resume-stream — bundle patch: activation row for the profile layer.
# This file is what makes the package installable via `dsh plugin add` and
# loadable as a profile bundle. The client half is declared by the
# `dsh.client` field in package.json (platform: web) + the `./client` export.
- insert:
    - id: '@omdp/dsh-resume-stream'
      name: '@omdp/dsh-resume-stream'
